// TODO: replace with the logged-in member's id once auth is wired up.
// All callers (Notifications, Calendar mutations, etc.) read from here so the
// auth swap is a one-line change.
export const CURRENT_MEMBER_ID = 1;
